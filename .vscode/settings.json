{    
    "rest-client.environmentVariables": {
        "$shared": {
            "HOST": "localhost",
            "PORT": "8025",
            "API_VERSION": "v1",          
            "API_PREFIX": "api"  
        }
    }
}
